import { Elysia } from 'elysia';
import type { StaticOptions } from './types';
export declare function staticPlugin<const Prefix extends string = '/prefix'>({ assets, prefix, staticLimit, alwaysStatic, ignorePatterns, headers: initialHeaders, maxAge, directive, etag: useETag, extension, indexHTML, detail, bunFullstack, decodeURI, silent }?: StaticOptions<Prefix>): Promise<Elysia>;
export default staticPlugin;
